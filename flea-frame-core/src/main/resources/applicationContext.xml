<?xml version="1.0" encoding="utf-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:cache="http://www.springframework.org/schema/cache"
	xsi:schemaLocation="
	http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
	http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
	http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
	http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache.xsd"
	default-autowire="byName" default-lazy-init="false">

	<!-- 自动扫描
	<context:component-scan base-package="com.huazie.**.dao.impl,com.huazie.**.service.impl,com.huazie.**.web" />-->

	<!-- 读取的配置文件 -->
	<bean id="propertyConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="locations"><!-- 单个配置文件使用location，且不用使用list，多个配置文件使用locations -->
			<list>
				<value>classpath:flea/db/flea-db-config.properties</value>
				<value>classpath:flea/cache/memcached.properties</value>
			</list>
		</property>
		<property name="fileEncoding" value="UTF-8" />
	</bean>

	<bean id="defaultPersistenceManager" class="org.springframework.orm.jpa.persistenceunit.DefaultPersistenceUnitManager">
		<property name="persistenceXmlLocations">
			<!-- 可以配置多个持久单元 -->
			<list>
				<value>classpath:META-INF/fleamarket-persistence.xml</value>
				<value>classpath:META-INF/fleamanagement-persistence.xml</value>
			</list>
		</property>
	</bean>

	<bean id="defaultPersistenceProvider" class="org.eclipse.persistence.jpa.PersistenceProvider" />

	<bean id="defaultLoadTimeWeaver" class="org.springframework.instrument.classloading.InstrumentationLoadTimeWeaver" />

	<bean id="defaultVendorAdapter" class="org.springframework.orm.jpa.vendor.EclipseLinkJpaVendorAdapter">
		<property name="showSql" value="true" />
	</bean>

	<bean id="defaultJpaDialect"
		class="org.springframework.orm.jpa.vendor.EclipseLinkJpaDialect" />

	<!-- ################################## -->
	<!-- FleaMarket TransAction Manager JPA -->
	<!-- ################################## -->
	<!-- <bean tempId="fleaMarketDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource"
		destroy-method="close">
		<property name="driverClass" value="${mysql.fleamarket.driver}" />
		<property name="jdbcUrl" value="${mysql.fleamarket.url}" />
		<property name="user" value="${mysql.fleamarket.user}" />
		<property name="password" value="${mysql.fleamarket.password}" />
	</bean> -->
	<bean id="fleaMarketEntityManagerFactory"
		class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
		<property name="persistenceUnitManager" ref="defaultPersistenceManager" />
		<property name="persistenceUnitName" value="fleamarket" />
		<!-- <property name="dataSource" ref="fleaMarketDataSource" /> -->
		<property name="persistenceProvider" ref="defaultPersistenceProvider" />
		<!-- <property name="loadTimeWeaver" ref="defaultLoadTimeWeaver" /> -->
		<property name="jpaVendorAdapter" ref="defaultVendorAdapter" />
		<property name="jpaDialect" ref="defaultJpaDialect" />
		<property name="jpaPropertyMap">
			<map>
				<entry key="eclipselink.weaving" value="false" />
			</map>
		</property>
	</bean>

	<bean id="fleaMarketTransactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
		<property name="entityManagerFactory" ref="fleaMarketEntityManagerFactory" />
	</bean>

	<tx:annotation-driven transaction-manager="fleaMarketTransactionManager" />

	<!-- ###################################### -->
	<!-- FleaManagement TransAction Manager JPA -->
	<!-- ###################################### -->
	<!-- <bean tempId="fleaManagementDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource"
		destroy-method="close">
		<property name="driverClass" value="${mysql.fleamanagement.driver}" />
		<property name="jdbcUrl" value="${mysql.fleamanagement.url}" />
		<property name="user" value="${mysql.fleamanagement.user}" />
		<property name="password" value="${mysql.fleamanagement.password}" />
	</bean> -->
	<bean id="fleaManagementEntityManagerFactory"
		class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
		<property name="persistenceUnitManager" ref="defaultPersistenceManager" />
		<property name="persistenceUnitName" value="fleamanagement" />
		<!-- <property name="dataSource" ref="fleaManagementDataSource" /> -->
		<property name="persistenceProvider" ref="defaultPersistenceProvider" />
		<!-- <property name="loadTimeWeaver" ref="defaultLoadTimeWeaver" /> -->
		<property name="jpaVendorAdapter" ref="defaultVendorAdapter" />
		<property name="jpaDialect" ref="defaultJpaDialect" />
		<property name="jpaPropertyMap">
			<map>
				<entry key="eclipselink.weaving" value="false" />
			</map>
		</property>
	</bean>

	<bean id="fleaManagementTransactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
		<property name="entityManagerFactory" ref="fleaManagementEntityManagerFactory" />
	</bean>

	<tx:annotation-driven transaction-manager="fleaManagementTransactionManager" />
	<!-- 导入调度任务 -->
	<!-- <import resource="spring-quartz.xml" /> -->

</beans>